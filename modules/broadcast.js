/*
  Module Handles Broadcasting Boss Status
*/

// On Boss Up Cast
exports.broadCastUp(boss){
  return new Promise(function(resolve, reject) {
    resolve([]);
  });
}

// On Boss Death Cast
exports.broadCastDown(boss, channel){
  return new Promise(function(resolve, reject) {
    resolve([]);
  });
}
